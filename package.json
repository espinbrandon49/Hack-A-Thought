{
  "name": "hack-a-thought-monolith",
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm install --prefix server && npm install --prefix client --include=dev && npm run build --prefix client",
    "start": "node server/server.js"
  },
  "engines": {
    "node": "18.x"
  }
}

